export const environment = {
  production: true,
  // Your Spring Boot URL (wherever you host the JAR, e.g., AWS, Render, etc.)
  // OR if you are using the PHP script for everything, put that base URL here.
  apiUrl: 'https://your-springboot-domain.com/api' 
};